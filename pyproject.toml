[project]
name = "sql-query-env"
version = "0.1.0"
description = "SQL Query Evaluator OpenEnv"
dependencies = [
    "openenv-core>=0.2.0",
    "fastapi",
    "uvicorn",
    "pydantic"
]

[project.scripts]
server = "server.app:main"
